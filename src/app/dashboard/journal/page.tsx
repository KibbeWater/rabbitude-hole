import DashboardHeader from '~/components/DashboardHeader';

export default async function Journal() {
    return <DashboardHeader name='journal' description={'discover and manage information\nyour r1 has saved'} />;
}
